@startuml TimeRange

!include ../../shared/DateTime/DateTime.pu
!include ./Duration/Duration.pu

class "TimeRange(時間帯)" as TimeRange <<ValueObject>> {
  + startAt: DateTime
  + endAt: DateTime
  --
  + duration: Duration
  + overlaps(other: TimeRange): boolean
  + contains(at: DateTime): boolean
}

TimeRange *-- DateTime
TimeRange *-- Duration

note right of TimeRange
  実装（TimeRange.ts）準拠
  - endAt は startAt + duration の派生（保持しない）
  - 予約は1時間単位のため、開始時刻は「ちょうど00分」のみ許容
  - 区間は [start, end)（endは含まない）
end note

@enduml
